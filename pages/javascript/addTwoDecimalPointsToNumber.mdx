# Add two decimal points to a number

```js copy
let foo = 1
foo.toFixed(2); // Adds 2 decimal points and converts the number to a string

console.log(foo) // '1.00'
```

[.toFixed() Reference](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/toFixed)